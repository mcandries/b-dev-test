{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Init venv",
            "type": "shell",
            "command": "python3.7 -m venv python3.7-venv"
        },
        {  
            "label": "Install modules",
            "type": "shell",
            "command": "source ${workspaceFolder}/python3.7-venv/bin/activate && pip install colored "
        }
    ]
}